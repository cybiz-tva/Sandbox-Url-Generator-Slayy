# Sandbox-Url-Generator-Slayy
An extension program of sandbox dashboard to generate Problem link with a unique problem ID for better tracking methods, 

Requires : 

npm install node-fetch uuid
npm install antd
npm install node-fetch uuid
